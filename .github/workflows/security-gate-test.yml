name: Security Gate Test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:

  security:

    name: Central Security Gate

    uses: TRM-MYOS/devsecops-security-platform/.github/workflows/security-scan.yml@main